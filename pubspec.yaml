name: audit_assistant
description: 审计助手 - 根据审计问题查找相关政策条款
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.2
  http: ^1.2.2
  shared_preferences: ^2.3.2

flutter:
  uses-material-design: true
